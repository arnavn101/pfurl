#!/usr/bin/env python3
#
# (c) 2017-2019 Fetal-Neonatal Neuroimaging & Developmental Science Center
#                     Boston Children's Hospital
#
#              http://childrenshospital.org/FNNDSC/
#                        dev@babyMRI.org
#

import json
import ast
from pfurl import args_help


if __name__ == '__main__':
    args = args_help.parser.parse_args()
    # TODO do we care about exit status?
    if args.desc:
        args_help.print_long_description()
    if args.synopsis:
        args_help.print_short_description()
    if args.man:
        args_help.print_man_page(args.man)
    if args.version:
        args_help.print_version()

    parameters = vars(args)

    # pfurl = pfurl.Pfurl(**parameters)
    # str_response = pfurl()
    #
    # try:
    #     # Is this string response a dictionary?
    #     d_response = ast.literal_eval(str_response)
    # except:
    #     d_response = str_response
    #
    # if not args.jsonpprintindent:
    #     print(str_response)
    # else:
    #     print(json.dumps(d_response, indent=int(args.jsonpprintindent)))
